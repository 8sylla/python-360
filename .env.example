# Copie ce fichier en .env et remplis les valeurs.
# .env est dans .gitignore : il ne doit JAMAIS être versionné.
# Ce fichier-ci, en revanche, est versionné — SANS valeurs réelles.

API_CLE=change-moi
DEBUG=true
FICHIER_DONNEES=donnees.json
